export const JwtConstants = {   
  //secret: process.env.JWT_SECRET || 'default_secret',
  //en produccion usar una variable de entorno para mayor seguridad
    secret: 'no utilizar esta palabra en produccion',
};